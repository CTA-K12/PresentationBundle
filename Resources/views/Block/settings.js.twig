<script type="text/javascript">
    // these should probably be set in config
    // but that is a task for another day.
    var maximized = 240;
    var minimized = 60;
    var xsMin     = 0;
    var smMin     = 768;
    var mdMin     = 992;
    var lgMin     = 1200;
    var baseUrl   = '{{app.request.getBasePath}}';

    // fraction of sidebar open percent where labels dissapear
    var threshold = 0.75;
    var quiet     =  100;
</script>
