{% trans_default_domain "mesd_presentation" %}

<div class="mesd_user_group_list">
    <ul>
    {% for group in groups %}
        <li><a href="{{ path('mesd_user_group_show', {'groupName': group.getName()} ) }}">{{ group.getName() }}</a></li>
    {% endfor %}
    </ul>
</div>
