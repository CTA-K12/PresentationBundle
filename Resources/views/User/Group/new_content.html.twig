{% trans_default_domain 'MesdUserBundle' %}

<form action="{{ path('mesd_user_group_new') }}" {{ form_enctype(form) }} method="POST" class="mesd_user_group_new">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'group.new.submit'|trans }}" />
    </div>
</form>
