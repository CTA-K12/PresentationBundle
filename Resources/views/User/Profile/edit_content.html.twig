{% trans_default_domain 'MesdUserBundle' %}

<form action="{{ path('mesd_user_profile_edit') }}" {{ form_enctype(form) }} method="POST" class="mesd_user_profile_edit">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
</form>
