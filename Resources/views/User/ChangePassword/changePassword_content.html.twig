{% trans_default_domain "mesd_presentation" %}

<form action="{{ path('mesd_user_change_password') }}" {{ form_enctype(form) }} method="POST" class="mesd_user_change_password">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'change_password.submit'|trans }}" />
    </div>
</form>
