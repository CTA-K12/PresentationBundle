{% trans_default_domain 'MesdUserBundle' %}

<form action="{{ path('mesd_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="mesd_user_registration_register">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'registration.submit'|trans }}" />
    </div>
</form>
