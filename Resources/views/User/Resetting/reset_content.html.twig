{% trans_default_domain 'MesdUserBundle' %}

<form action="{{ path('mesd_user_resetting_reset', {'token': token}) }}" {{ form_enctype(form) }} method="POST" class="mesd_user_resetting_reset">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'resetting.reset.submit'|trans }}" />
    </div>
</form>
